apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: argo-app
  template:
    metadata:
      labels:
        app: argo-app
    spec:
      containers:
        - name: argo-app-container
          image: ghcr.io/ghcr.io/ghcr.io/ghcr.io/eldardl/argocd-ci-cd-pipeline:9038771379029a670af718540abea5bce709ae89
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: ghcr-docker-secret
